// 260315 c. hausch

TriMix(a, b, c, pos) {
	t0 = clamp(pos, 0, 1);
	t1 = clamp(pos - 1, 0, 1);
	blend01 = mix(a, b, t0);
	blend12 = mix(b, c, t1);
	segment = clamp(floor(pos), 0, 1);
	return mix(blend01, blend12, segment);
}
